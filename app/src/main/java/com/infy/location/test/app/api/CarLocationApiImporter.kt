package com.infy.location.test.app.api

import com.infy.location.test.app.models.CarLocationData

// CarLocationApiImporter is responsible to load algorithm and process location data.
class CarLocationApiImporter {

    init {
        // Initialize Algorithm/API to process location data to get location result.
    }

    fun processCarLocationData(carLocationData: CarLocationData): String {
        // @carLocationData contains car model and location details.
        // share carLocationData to algorithm or API.
        // call specific external API to get location result.
        return ""
    }
}
